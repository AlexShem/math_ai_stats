---
title: "Математическое ожидание и стандартное отклонение"
subtitle: "Презентация"
format:
    html:
        monofont: JetBrains Mono
        number-sections: false
        html-math-method: katex
        self-contained: true
        code-overflow: wrap
        code-tools: true
        code-link: true
        toc: true
jupyter: python3
---

# Математическое ожидание и стандартное отклонение

## 🎓 1. Введение

В этой презентации мы разберём:

- Что такое математическое ожидание
- Что такое стандартное отклонение
- Как они считаются
- Зачем они нужны в жизни
- Примеры
- Реализация на Python

---

## 📐 2. Математическое ожидание (среднее значение)

### Что это?

Это среднее значение, которое мы ожидаем от случайной величины.

📌 Простой пример:
Если бросить игральный кубик (с числами от 1 до 6), то математическое ожидание = среднее значение всех возможных исходов:

$$
M = \frac{1 + 2 + 3 + 4 + 5 + 6}{6} = 3.5
$$

### Формула:

$$
M(X) = \sum (x_i \cdot p_i)
$$

где

- $x_i$ — возможное значение  
- $p_i$ — вероятность этого значения  

---

## 📊 3. Стандартное отклонение

### Что это?

Это мера разброса данных — насколько сильно значения отклоняются от среднего.

📌 Пример:

- Если оценки ученика: 4, 5, 4, 5 — то разброс маленький.
- А если 2, 5, 1, 6 — то разброс большой.

### Формула:

$$
\sigma = \sqrt{\frac{1}{n} \sum (x_i - M)^2}
$$

где  

- $x_i$ — значения  
- $M$ — среднее значение  
- $n$ — количество значений

---

## 🌍 4. Применение в реальной жизни

🔹 Финансы: оценка средней доходности акций и рисков (стандартное отклонение).

🔹 Образование: анализ успеваемости — средняя оценка и разброс.

🔹 Игры и случайности: просчёт вероятностей выигрыша.

🔹 Производство: контроль качества — насколько отклоняются размеры деталей от нормы.

---

## 🧮 5. Примеры

### Пример 1: Оценки ученика

Оценки: 3, 4, 5

Математическое ожидание:

$$
M = \frac{3 + 4 + 5}{3} = 4
$$

Стандартное отклонение:

$$
\sigma = \sqrt{\frac{(3 - 4)^2 + (4 - 4)^2 + (5 - 4)^2}{3}} = \sqrt{\frac{1 + 0 + 1}{3}} = \sqrt{\frac{2}{3}} \approx 0.82
$$

---

## 💻 6. Пример реализации на Python

```{python}
import numpy as np

# Пример данных
grades = [3, 4, 5]

# Математическое ожидание (среднее)
mean = np.mean(grades)

# Стандартное отклонение
std_dev = np.std(grades)

print("Среднее значение:", mean)
print("Стандартное отклонение:", std_dev)
```

Результат:

```text
Среднее значение: 4.0  
Стандартное отклонение: 0.8165
```

---

## ✅ 7. Вывод

- Математическое ожидание показывает "средний результат".
- Стандартное отклонение показывает "насколько данные отличаются от среднего".
- Эти понятия помогают анализировать данные, оценивать риски и принимать решения.
